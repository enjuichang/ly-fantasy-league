name: Refresh Data

on:
  schedule:
    # Run at 6:00 AM and 6:00 PM UTC daily
    - cron: '0 6,18 * * *'
  workflow_dispatch: # Allow manual triggering

jobs:
  refresh:
    runs-on: ubuntu-latest
    steps:
      - name: Sync Rollcall Votes
        run: |
          curl -X GET "${{ secrets.APP_URL }}/api/refresh-data?type=rollcall" \
          -H "Authorization: Bearer ${{ secrets.CRON_SECRET }}" \
          --fail

      - name: Sync Propose Bills (Batch 1/4)
        run: |
          curl -X GET "${{ secrets.APP_URL }}/api/refresh-data?type=propose&limit=30&offset=0" \
          -H "Authorization: Bearer ${{ secrets.CRON_SECRET }}" \
          --fail

      - name: Sync Propose Bills (Batch 2/4)
        run: |
          curl -X GET "${{ secrets.APP_URL }}/api/refresh-data?type=propose&limit=30&offset=30" \
          -H "Authorization: Bearer ${{ secrets.CRON_SECRET }}" \
          --fail

      - name: Sync Propose Bills (Batch 3/4)
        run: |
          curl -X GET "${{ secrets.APP_URL }}/api/refresh-data?type=propose&limit=30&offset=60" \
          -H "Authorization: Bearer ${{ secrets.CRON_SECRET }}" \
          --fail

      - name: Sync Propose Bills (Batch 4/4)
        run: |
          curl -X GET "${{ secrets.APP_URL }}/api/refresh-data?type=propose&limit=30&offset=90" \
          -H "Authorization: Bearer ${{ secrets.CRON_SECRET }}" \
          --fail

      - name: Sync Cosign Bills (Batch 1/4)
        run: |
          curl -X GET "${{ secrets.APP_URL }}/api/refresh-data?type=cosign&limit=30&offset=0" \
          -H "Authorization: Bearer ${{ secrets.CRON_SECRET }}" \
          --fail

      - name: Sync Cosign Bills (Batch 2/4)
        run: |
          curl -X GET "${{ secrets.APP_URL }}/api/refresh-data?type=cosign&limit=30&offset=30" \
          -H "Authorization: Bearer ${{ secrets.CRON_SECRET }}" \
          --fail

      - name: Sync Cosign Bills (Batch 3/4)
        run: |
          curl -X GET "${{ secrets.APP_URL }}/api/refresh-data?type=cosign&limit=30&offset=60" \
          -H "Authorization: Bearer ${{ secrets.CRON_SECRET }}" \
          --fail

      - name: Sync Cosign Bills (Batch 4/4)
        run: |
          curl -X GET "${{ secrets.APP_URL }}/api/refresh-data?type=cosign&limit=30&offset=90" \
          -H "Authorization: Bearer ${{ secrets.CRON_SECRET }}" \
          --fail

      - name: Sync Written Interpellations (Batch 1/12)
        run: |
          curl -X GET "${{ secrets.APP_URL }}/api/refresh-data?type=written_interpellation&limit=10&offset=0" \
          -H "Authorization: Bearer ${{ secrets.CRON_SECRET }}" \
          --fail

      - name: Sync Written Interpellations (Batch 2/12)
        run: |
          curl -X GET "${{ secrets.APP_URL }}/api/refresh-data?type=written_interpellation&limit=10&offset=10" \
          -H "Authorization: Bearer ${{ secrets.CRON_SECRET }}" \
          --fail

      - name: Sync Written Interpellations (Batch 3/12)
        run: |
          curl -X GET "${{ secrets.APP_URL }}/api/refresh-data?type=written_interpellation&limit=10&offset=20" \
          -H "Authorization: Bearer ${{ secrets.CRON_SECRET }}" \
          --fail

      - name: Sync Written Interpellations (Batch 4/12)
        run: |
          curl -X GET "${{ secrets.APP_URL }}/api/refresh-data?type=written_interpellation&limit=10&offset=30" \
          -H "Authorization: Bearer ${{ secrets.CRON_SECRET }}" \
          --fail

      - name: Sync Written Interpellations (Batch 5/12)
        run: |
          curl -X GET "${{ secrets.APP_URL }}/api/refresh-data?type=written_interpellation&limit=10&offset=40" \
          -H "Authorization: Bearer ${{ secrets.CRON_SECRET }}" \
          --fail

      - name: Sync Written Interpellations (Batch 6/12)
        run: |
          curl -X GET "${{ secrets.APP_URL }}/api/refresh-data?type=written_interpellation&limit=10&offset=50" \
          -H "Authorization: Bearer ${{ secrets.CRON_SECRET }}" \
          --fail

      - name: Sync Written Interpellations (Batch 7/12)
        run: |
          curl -X GET "${{ secrets.APP_URL }}/api/refresh-data?type=written_interpellation&limit=10&offset=60" \
          -H "Authorization: Bearer ${{ secrets.CRON_SECRET }}" \
          --fail

      - name: Sync Written Interpellations (Batch 8/12)
        run: |
          curl -X GET "${{ secrets.APP_URL }}/api/refresh-data?type=written_interpellation&limit=10&offset=70" \
          -H "Authorization: Bearer ${{ secrets.CRON_SECRET }}" \
          --fail

      - name: Sync Written Interpellations (Batch 9/12)
        run: |
          curl -X GET "${{ secrets.APP_URL }}/api/refresh-data?type=written_interpellation&limit=10&offset=80" \
          -H "Authorization: Bearer ${{ secrets.CRON_SECRET }}" \
          --fail

      - name: Sync Written Interpellations (Batch 10/12)
        run: |
          curl -X GET "${{ secrets.APP_URL }}/api/refresh-data?type=written_interpellation&limit=10&offset=90" \
          -H "Authorization: Bearer ${{ secrets.CRON_SECRET }}" \
          --fail

      - name: Sync Written Interpellations (Batch 11/12)
        run: |
          curl -X GET "${{ secrets.APP_URL }}/api/refresh-data?type=written_interpellation&limit=10&offset=100" \
          -H "Authorization: Bearer ${{ secrets.CRON_SECRET }}" \
          --fail

      - name: Sync Written Interpellations (Batch 12/12)
        run: |
          curl -X GET "${{ secrets.APP_URL }}/api/refresh-data?type=written_interpellation&limit=10&offset=110" \
          -H "Authorization: Bearer ${{ secrets.CRON_SECRET }}" \
          --fail

      - name: Sync Floor Speeches
        run: |
          curl -X GET "${{ secrets.APP_URL }}/api/refresh-data?type=floor_speech" \
          -H "Authorization: Bearer ${{ secrets.CRON_SECRET }}" \
          --fail
